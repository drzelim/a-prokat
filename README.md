### Открыть директорию `front` и установить зависимости:
> npm i

### Потом также установить зависимости в директории `server`:
> npm i

### Запустить `server`:
> npm run start 
> 
или
> 
> npm run start:dev

### Запустить `front`:
> npm run serve
